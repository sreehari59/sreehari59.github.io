# URL of the deployed Cloudflare Worker chat endpoint (shared with the /portfolio site).
# Inlined at build time (NEXT_PUBLIC_*). If unset, chat runs canned-only — the site still works.
# e.g. https://sreehari-portfolio-chat.<your-subdomain>.workers.dev
NEXT_PUBLIC_CHAT_URL=
